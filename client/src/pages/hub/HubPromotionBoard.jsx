import PromotionBoard from "../../components/promotions/PromotionBoard";

/**
 * The Staff Hub's promotion board — nominations across the staff ladder and the
 * timed vote behind each one.
 */
export default function HubPromotionBoard() {
  return <PromotionBoard />;
}
